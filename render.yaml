services:
  - type: web
    name: hotel-iq
    env: node
    buildCommand: npm run railway:build
    startCommand: node src/backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
